# St. Augustine TimeQuest — Backend Dependencies

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
geoalchemy2==0.14.3

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained) reads bcrypt's removed __about__.__version__
# attribute for backend detection; bcrypt>=4.1 dropped it and breaks every
# password hash/verify call. Pin to the last compatible release.
bcrypt==4.0.1

# AI / Vector search
qdrant-client==1.7.3
httpx==0.26.0

# Document processing
pdfplumber==0.10.3
pypdf==4.0.1
beautifulsoup4==4.12.3
lxml==5.1.0

# Image processing
Pillow==10.2.0

# Rate limiting
slowapi==0.1.9

# Utilities
python-dotenv==1.0.1
pydantic==2.6.0
pydantic-settings==2.1.0
email-validator==2.1.0.post1

# Storage (MinIO / S3-compatible)
boto3==1.34.28

# Testing
pytest==8.0.0
pytest-asyncio==0.23.3
httpx==0.26.0
