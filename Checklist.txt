# St. Augustine TimeQuest — Feature Checklist

Compiled from the three source documents (St. Augustine AI Adventure App
plan, "What You Need to Build the First Version," and the Let's Roam
reference). Organized by build stage. `[x]` = scaffolded in the current
repo. `[ ]` = still to build.

---

## Stage 1 — Project Foundation

- [x] Git repository with the recommended folder structure — public on GitHub at kemperdesign/ttt-scavenger-hunt-v3 (made public to work around Vercel Hobby's forced deployment-protection wall; safe, no secrets ever committed)
- [x] Next.js + TypeScript frontend project
- [x] FastAPI backend project
- [x] PostgreSQL/PostGIS-ready SQLAlchemy models
- [x] Environment configuration (`.env.example` for both frontend/backend) — both exist and are complete; fixed `CORS_ORIGINS` to use valid JSON syntax (pydantic-settings requires it for `List[str]` fields — a bare string crashes the app on startup). Real production `.env` was hand-created on the server only, never committed (correct — see Deployment Notes below)
- [ ] Alembic migration scaffold — present but unused so far; production tables were created directly from `Base.metadata.create_all` (no versioned migrations exist yet in `alembic/versions/`)
- [x] Actual PostgreSQL + PostGIS instance provisioned and migrated — running on Linode, seeded with 1 adventure / 5 stops
- [ ] Development launch scripts (one-command "start everything" script)
- [ ] `LICENSE` file

## Stage 2 — Adventure Engine

- [x] Adventure schema — verified in `app/models/adventure.py`: title, slug, description, short_description, difficulty, estimated_duration_minutes, start_lat/lng, total_points, tags, is_published, is_featured. Note: no "price" or "status" enum as originally envisioned — the real schema uses `is_published`/`is_featured` booleans instead
- [x] Stops schema — verified: title, lat/lng, gps_radius_meters, order_index, points, hint_text, ai_character_id, historical_content
- [x] Challenge schema + all 8 challenge type enums — verified in `app/api/challenges.py`: real evaluation logic exists for multiple_choice, text_answer, sequence_puzzle, qr_code, gps_checkin, and photo_submission. `ai_conversation` and `branching_story` both just return an automatic success (open-ended by design) — branching_story's actual choice-tree UI lives client-side in `BranchingStoryChallenge.tsx`, not validated server-side
- [x] Scoring (`points_awarded` on each attempt) — verified
- [x] Progress tracking (`GameSession` + `ChallengeAttempt` tables) — verified. Fixed a real dead-end bug: checking into the last stop just re-showed the same "Check In Here" screen forever (the index-advance logic clamped at `stops.length - 1`, so the last stop could never trigger completion). Now calls `completeSession()` and navigates to the completion page. Also fixed the completion page reading its session ID from an unused `localStorage` key instead of the IndexedDB key the start page actually writes to — badges never loaded there either.
- [ ] Test-data importer (`scripts/import_adventure.py`) — does not exist; only `seed_adventures.py` exists, which seeds one hardcoded adventure, not a generic importer for arbitrary content
- [x] Sample adventure content, 5 stops — real seeded adventure exists: **"Founding St. Augustine: 1565"** (not "Secrets of St. Augustine" as originally named — content differs from the original plan, but a complete working sample with 5 real stops exists and is live in production)
- [x] Sequence-puzzle challenge validation logic — verified: real order-comparison logic (`user_order == correct_order`)
- [x] QR-code challenge validation logic — verified: real code-comparison logic (`scanned_code == expected`)
- [x] Branching-story logic (choice → next stop/challenge) — `BranchingStoryEditor.tsx` (admin) lets admins build a node/choice graph with a designated start node; `BranchingStoryChallenge.tsx` (player) navigates it and auto-completes on reaching an end node (zero choices); backend evaluator accepts completion submissions
- [x] Hint system exposed in the player UI — `HintButton.tsx` verified wired into the adventure start page (two-tap confirm, deducts points)
- [x] Achievement badges (schema + award logic) — `Badge`/`UserBadge` models + `/badges/check/{session_id}` endpoint verified
- [x] Leaderboard endpoint (cross-team ranking) — `/adventures/{id}/leaderboard?type=player|team` now supports both; team mode aggregates via `TeamMember` LEFT OUTER JOIN with member counts. Frontend leaderboard page has a Players/Teams toggle

## Stage 3 — Map and GPS

- [x] Interactive map (MapLibre GL) — `AdventureMap.tsx` was fully built but never actually imported/rendered anywhere in the app; wired into the player start page. MapTiler key is now live in Cloudflare Production (an earlier copy-paste mistake included the literal word "Key" as part of the value, causing silent 403s — fixed)
- [x] Stop markers — verified (numbered, color-coded by completed/current/upcoming)
- [x] "Check in here" button calling the geofence endpoint — verified, wired to `/gps/checkin`
- [x] Server-side geofence validation (haversine distance, not client-trusted) — verified in `app/api/gps_checkin.py`
- [x] Live player position marker on the map — verified
- [x] Simulated GPS mode — `SimulatedGPS.tsx` verified, gated by `ALLOW_SIMULATED_GPS` env var
- [x] Distance/direction indicator — `DistanceIndicator.tsx` now shows a rotating arrow + compass text ("head northeast") alongside distance and proximity color, using a new `bearing()` helper (initial great-circle bearing) next to the existing `haversine()` distance calc. North-up convention, no device-orientation permission needed.
- [x] Street-crossing / route-safety warnings — new admin-curated `safety_warning` field per stop, shown as a caution banner on the player page while at/approaching that stop. Deliberately not auto-generated (no real routing engine exists to detect actual crossings — see the straight-line map note above); content still needs to be filled in per stop from an in-person walk (see Stage 7 Field Test)
- [x] Day/night route indicators — `TimeOfDayBanner` + `useTimeOfDay` + map theme switching verified

## Stage 4 — Administrator Portal

- [x] Admin login UI — verified working end-to-end this session (tested real login, fixed the bcrypt bug that was silently blocking it, fixed an infinite-redirect-loop bug on this exact page)
- [x] Adventure editor — verified: create/edit description, duration, difficulty, tags, featured flag, publish/unpublish/delete
- [x] Stop editor — lat/lng, GPS radius, historical text, points, hint text, reorder via order_index all verified. Images/audio now have a real upload widget (`MediaUploadField.tsx`): file picker with live preview, validated + stored via MinIO (`POST /stops/{id}/media`), replacing the old plain URL text fields
- [x] Challenge editor — full CRUD (list/create/update/delete) now on the backend; `ChallengeConfigEditor.tsx` gives per-type field UI for all 8 types (multiple-choice options + correct answer, accepted text answers, sequence items, QR code + hint, AI character + min exchanges + topic, branching-story graph editor, photo review toggle + subject hint, GPS radius override), plus editable title/prompt
- [x] AI character creator — characters moved from a hardcoded Python dict into a DB-backed `ai_characters` table (migration 003, seeded with the same 7 characters). `admin/characters` now has full CRUD: create/edit/delete via `CharacterEditor.tsx` (name, era, personality, system prompt, uncertainty phrase, greeting, comma-separated knowledge source topics). Character ID stays locked after creation since challenge configs and `stop.ai_character_id` reference it by string. Knowledge-source *topic assignment* is covered (the `source_topics` field feeds RAG retrieval); a UI for browsing/assigning actual ingested documents doesn't exist yet — that depends on the RAG source-ingestion pipeline in Stage 5, which is still unbuilt
- [x] Photo-submission review queue — verified working (approve/reject)
- [x] Player analytics dashboard — verified (leaderboard per adventure)
- [x] Preview-as-player mode — "▶ Preview as Player" button on the admin adventure page opens the normal player flow (new tab) with a non-persisted, leaderboard-excluded session; auto-teleports simulated GPS to the current stop plus a "Skip Stop" control, so a full walkthrough works from a desk with no real movement
- [x] Simulated-location testing controls (admin-side) — covered by preview mode's auto-GPS-teleport + skip-stop control above, scoped to admin-only sessions
- [ ] "Reset adventure" testing control — not built (preview sessions are already fresh/disposable each time, so lower priority, but no explicit reset button exists for a player's real session)
- [ ] Award-test-points control — not built
- [ ] View AI retrieval sources (debug panel) — not built
- [x] User management / permissions — `admin/users` page: list users, promote/demote admin, deactivate/reactivate account. Self-lockout guarded (can't remove your own admin access or deactivate yourself). Logged to the audit trail.
- [x] Audit log viewer — `admin/audit-log` page surfaces the existing (previously unused) `AuditLog` backend/endpoint; user-permission changes and bulk submission reviews now write entries
- [x] Bulk content moderation — photo submissions page: select-all + per-item checkboxes, bulk approve/reject via new `POST /submissions/bulk-review`

## Stage 5 — AI Historian (RAG)

- [x] AI conversation challenge chat — structured per-challenge chat (`/challenges/{id}/chat`) now runs on **OpenAI (gpt-4o-mini)**, not Ollama, since the 2GB VPS can't run a local model. Persists message history to `conversation_messages`, auto-completes at `min_exchanges`, awards points server-side. Live in production.
- [ ] Ollama actually running in production — still NOT deployed (2GB VPS insufficient); the *separate*, older `/api/v1/characters/chat` endpoint (open-ended chat, not tied to a specific challenge — see `AIChatChallenge.tsx`, currently unused in any page) still targets Ollama and will fail if ever wired up. Either point it at OpenAI too, or retire it in favor of the new challenge-based chat.
- [ ] RAG query pipeline skeleton (`app/ai/rag.py`) — Ollama + Qdrant
- [ ] Character chat API endpoint with source citations + uncertainty flag
- [ ] One sample character (Spanish colonial guide) with a scoped system prompt
- [ ] Source ingestion pipeline (chunking + embedding of approved documents)
- [ ] Source register / metadata tracking (title, author, date, rights, reliability, review status)
- [ ] Qdrant collection actually populated with St. Augustine sources
- [ ] Additional characters: Henry Flagler, Pedro Menéndez de Avilés, Gilded Age hotel guest, pirate captain, colonial shopkeeper, civil-rights-era guide, Timucua cultural interpreter (needs historian consultation)
- [ ] Prompt-injection / guardrail testing
- [ ] Fallback/uncertainty response tuning
- [ ] "Learn more" source panel in the player UI

## Stage 6 — Mobile / PWA Experience

- [x] Installable PWA manifest — icon files it referenced (`icon-192.png`, `icon-512.png`, favicon) never actually existed (404s on every load); added a simple hourglass mark matching the app's amber/slate theme
- [x] Mobile-responsive Tailwind layout
- [x] Service worker for offline caching — `sw.js` verified: cache-first static assets, LRU tile cache (500 entries), network-first API with stale-while-revalidate fallback, offline page fallback
- [x] Camera upload flow for photo challenges — `PhotoChallengeCapture.tsx` verified (camera/file picker, preview, 10MB validation)
- [x] IndexedDB local progress caching — `useIndexedDB`/`useOfflineSync` hooks verified actually used in the adventure start page (not just scaffolded and unused)
- [x] Accessibility pass — `AccessibilityContext` + `AccessibilityPanel` verified: font size (normal/large/x-large), high contrast, reduce motion, persisted to localStorage
- [x] Solo vs. team selection flow — `TeamSetup.tsx` (see team join-code entry below) shown before every new adventure session
- [ ] Safety/location-permission notice screen — not built; the app requests location directly via the browser's native permission prompt, no custom pre-permission explainer screen first
- [x] Team join-code UI — `TeamSetup.tsx` shown before a new session starts: play solo, create a team (join code shown prominently to share), or join by entering a code. Also fixed the `Team` frontend type, which previously didn't match the backend response shape at all.

## Stage 7 — Field Test

- [ ] Walk the full 5-stop route in person; verify GPS accuracy at each radius
- [ ] Test on iPhone and Android
- [ ] Test on cellular and Wi-Fi
- [ ] Test in bright sunlight (screen visibility)
- [ ] Test offline recovery
- [ ] Verify sidewalk accessibility, crossings, hours, construction at each stop
- [ ] Confirm average walking time matches the 45–60 min estimate
- [ ] Team-joining test with multiple devices
- [ ] AI response accuracy review by a historical reviewer

---

## Content To Produce (per stop, all 5 stops)

- [ ] 150-word summary + 400-word extended history per stop
- [ ] Key dates, people, documented facts, common myths
- [ ] Audio narration script (+ recorded audio)
- [ ] Historic photograph + cover photograph + map marker image
- [ ] Character portraits
- [ ] Achievement badge art
- [ ] Second trivia question per stop (spec calls for two; sample has one)
- [ ] Bonus/optional challenge per stop
- [ ] Final "decode the phrase" end-game challenge using collected answers

## Historical Source Register

- [ ] Catalog existing St. Augustine RAG sources with: title, author, date, URL, rights status, reliability, locations covered, period, review status, reviewer
- [ ] Historian/community review pass before publishing sensitive content (esp. Civil Rights Journey adventure, Timucua character)

## Security & Privacy

- [x] Password hashing (bcrypt) and JWT auth (backend) — was scaffolded but silently non-functional in production; fixed and verified working end-to-end (see Deployment Notes: bcrypt/passlib pin)
- [x] Delete endpoints actually delete — `db.delete(obj)` without `await` on `AsyncSession` (unlike sync `Session.delete()`, the async version is a coroutine) silently no-opped: 200 OK returned, nothing ever deleted. Affected challenge/stop/adventure/account delete. Fixed across all four by awaiting the call; verified via direct DB query that deletes now persist.
- [x] HTTPS in production — frontend via Cloudflare Pages (automatic), backend via Caddy + Let's Encrypt on a free nip.io hostname (see Deployment Notes)
- [ ] Rate limiting — slowapi middleware is wired up in code; not load-tested against the live deployment yet
- [ ] Upload file validation + photo size limits
- [ ] Location-data retention policy (store check-in result/time only, not continuous tracks)
- [x] Account deletion + data export — `/account/export` and `/account/delete` verified; account delete was one of the four endpoints hit by the AsyncSession.delete() bug above, now fixed
- [x] Audit logs — backend (`AuditLog` model + `/admin/audit` endpoint) existed but had no admin UI and almost nothing wrote to it; now viewable at `admin/audit-log`, and user-permission changes + bulk submission reviews log entries. Most other admin actions (adventure/stop/challenge CRUD) still don't log — only user management and bulk moderation do so far.
- [ ] Automated database + media backups
- [ ] AI prompt-injection controls
- [ ] Content moderation for user-submitted photos
- [ ] Child-use policy
- [ ] Privacy policy and terms pages

## Accounts / Infrastructure (needed before public launch)

- [x] GitHub repo — public, not private (required by Vercel Hobby's deployment protection; harmless since no secrets are committed — see Deployment Notes)
- [ ] Domain name (trademark search first) — not purchased yet; currently running on free subdomains (`*.pages.dev` for frontend, `*.nip.io` for backend) which is fine for testing but should be replaced before public launch
- [ ] Project email address
- [x] Hosting provider / Linux VPS — Linode 2GB Nanode, Docker Compose, see Deployment Notes for exact stack
- [ ] Transactional email provider
- [ ] Stripe (only once paid adventures launch)
- [ ] Error-monitoring service
- [ ] Analytics (PostHog + Metabase or equivalent)
- [x] Map tile provider — MapTiler key configured in Cloudflare Production and confirmed rendering real tiles live
- [x] Object storage (MinIO locally / S3-compatible in production) — MinIO running on the Linode box, bucket auto-created on boot

## People / Roles To Cover

- [ ] Product owner decisions locked (audience, branding, business model)
- [ ] Developer (frontend/backend/deploy) — in progress
- [ ] Instructional/experience designer pass (challenge sequencing, difficulty, hints, engagement)
- [ ] Independent historical reviewer
- [ ] Field tester

---

## Postponed by Design (do not build yet)

- [ ] Native iOS / Android apps
- [ ] Full AR / camera-based landmark recognition
- [ ] Real-time multiplayer movement
- [ ] Automatic AI photo-challenge judging
- [ ] Voice character conversations
- [ ] Cryptocurrency rewards
- [ ] Facial recognition
- [ ] Multi-city marketplace / white-label accounts
- [ ] School LMS integrations
- [ ] Dynamic AI-generated personalized routes
- [ ] Corporate dashboards / business subscriptions

## Future Adventures (beyond the first prototype)

- [ ] The Flagler Challenge
- [ ] Haunted St. Augustine
- [ ] Pirate Pursuit
- [ ] Civil Rights Journey (requires historian + community review before publication)

## Deployment Notes — What's Actually Live, And What To Reuse

The app is deployed and reachable end-to-end for testing. Concrete state:

**Frontend:** https://ttt-scavenger-hunt-v3.pages.dev (Cloudflare Pages, free tier)
**Backend:** https://173-230-128-241.nip.io (Linode 2GB VPS, Docker Compose, Caddy reverse proxy)
**Admin login:** kemperdesignservices@gmail.com / 4173 (weak on purpose for a private test box — change before this ever becomes public)

Working config, in case the next project needs the same stack:

- **Cloudflare Pages + Next.js**: build command `npx @cloudflare/next-on-pages@1`, output dir `.vercel/output/static`, root dir = your frontend folder. Needs a `.npmrc` with `legacy-peer-deps=true` (Cloudflare's own toolchain has an internal version conflict otherwise) and the `nodejs_compat` compatibility flag set on *both* Production and Preview environments (Settings → Runtime).
- **No domain was purchased.** Backend gets a real hostname for free via `nip.io` (wildcard public DNS: `173-230-128-241.nip.io` resolves straight to that IP, no signup needed) plus Caddy for automatic free HTTPS. This was required, not optional — Cloudflare Workers refuse to `fetch()` a bare IP address.
- **GitHub repo had to be public**, not private, because Vercel's Hobby tier forces a login wall on private-repo deployments that can't be disabled without a paid plan. (We ultimately moved off Vercel to Cloudflare Pages anyway, which doesn't have this restriction — but the repo is already public and there's no reason to flip it back.)
- A full reusable write-up of every deployment gotcha we hit (Cloudflare build settings, the bcrypt/passlib version trap, SQLAlchemy circular-import patterns, the AuthGuard-guards-its-own-login-page loop, etc.) is saved separately at `deployment-playbook-notes.md` on the Desktop — read that before starting the next project's deployment.

## Revenue Model (post-MVP)

- [ ] Paid premium adventures / family & group passes / annual membership
- [ ] Corporate team-building packages
- [ ] School program pricing + teacher dashboard
- [ ] Sponsored stops / partner QR-code offers (schema exists via `partners` table; no admin UI or redemption flow yet)
- [ ] Private event booking (birthdays, proposals, bachelor/ette, conferences)

## Future App Idea: Wine Passport (separate concept, not part of TimeQuest)

A personal wine-tracking app — logging bottles tried, where and when, with
ratings and photos. Distinct product from TimeQuest; captured here so the
idea isn't lost.

**Core features (as requested):**
- [ ] Wine log — track every wine tried, with date and notes
- [ ] Location tagging — where each wine was had (restaurant, winery, home, etc.)
- [ ] Rating system — personal score/review per wine
- [ ] Label photo capture — snap a photo of the bottle label per entry
- [ ] "Where to buy" lookup — locate retailers or sources carrying a given wine

**Additional feature ideas:**
- [ ] Label OCR — auto-extract producer/vintage/varietal from a label photo instead of typing it in
- [ ] Barcode/QR scan — quick lookup against a wine database instead of manual entry
- [ ] Tasting notes template — structured fields (nose, palate, finish, tannins, acidity) beyond a free-text note
- [ ] Food pairing suggestions — per wine or varietal
- [ ] Personal cellar/inventory tracking — bottles owned vs. bottles already had, with quantity and storage location
- [ ] Drink-window reminders — notify when an aging bottle is approaching its ideal drinking window
- [ ] Price tracking — what was paid vs. current market price
- [ ] Wine region exploration — map view of regions/wineries visited or wines tried from
- [ ] Social features — share tasting notes or a passport profile with friends, compare ratings
- [ ] Wishlist — wines to try, separate from the log of wines already had
- [ ] Achievement badges — e.g. "tried 10 varietals," "visited 5 regions" (same pattern as TimeQuest's badge system)
- [ ] Export/share the passport — PDF or shareable link of a personal wine history
- [ ] AI sommelier chat — ask for recommendations based on logged preferences (same pattern as TimeQuest's AI historian characters)